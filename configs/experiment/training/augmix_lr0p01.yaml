name: augmix_resnet18_lr0p01
stage: training
includes:
  dataset: cifar10
  model: resnet18
  train_aug: augmix
  test_adapt: none
training:
  out_dir: outputs/experiments/training
  experiment_name: augmix_resnet18_lr0p01
  epochs: 20
  optimizer: sgd
  lr: 0.01
  scheduler: cosine
  seed: 42
  device: auto

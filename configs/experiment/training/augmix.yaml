name: augmix_resnet18
stage: training
includes:
  dataset: cifar10
  model: resnet18
  train_aug: augmix
  test_adapt: none
training:
  out_dir: outputs/experiments/training
  experiment_name: augmix_resnet18
  epochs: 20
  optimizer: sgd
  scheduler: cosine
  seed: 42
  device: auto
